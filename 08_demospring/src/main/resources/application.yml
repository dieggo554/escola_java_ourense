spring:
  application:
    name: 08_demospring
  datasource: 
    url: jdbc:mysql://127.0.0.1:3306/db_banco
    username: root
    password: abc123.
# Esto le dirá a Hibernate que se encargue
# de crear la tabla
  jpa:
    hibernate:
      ddl-auto: create